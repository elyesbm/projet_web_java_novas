<nav class="fixed top-0 w-full z-50 border-b border-white/10 bg-background/80 backdrop-blur-xl">
    <div class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
        <a href="{{ path('app_home') }}" class="flex items-center gap-3 group">
            <img src="{{ asset('images/logo.png') }}" alt="Novas" class="h-10 w-auto">
        </a>

        <div class="hidden md:flex items-center gap-8">
            <a href="{{ path('app_home') }}#about" class="text-sm text-muted-foreground hover:text-foreground transition-colors">A propos</a>
            <a href="{{ path('app_home') }}#features" class="text-sm text-muted-foreground hover:text-foreground transition-colors">Fonctionnalites</a>
            <a href="{{ path('app_home') }}#testimonials" class="text-sm text-muted-foreground hover:text-foreground transition-colors">Temoignages</a>
        </div>

        <div class="flex items-center gap-4">
            {% if app.user %}
                <a href="{{ path('app_user_profile') }}" class="hidden sm:block text-sm text-muted-foreground hover:text-foreground transition-colors px-4 py-2">
                    Mon compte
                </a>
                <a href="{{ path('app_logout') }}" class="text-sm font-medium bg-primary text-white px-5 py-2.5 rounded-xl hover:bg-primary/90 transition shadow-lg shadow-primary/25">
                    Deconnexion
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" class="hidden sm:block text-sm text-muted-foreground hover:text-foreground transition-colors px-4 py-2">
                    Se connecter
                </a>
                <a href="{{ path('app_register') }}" class="text-sm font-medium bg-primary text-white px-5 py-2.5 rounded-xl hover:bg-primary/90 transition shadow-lg shadow-primary/25">
                    Commencer
                </a>
            {% endif %}
        </div>
    </div>
</nav>
