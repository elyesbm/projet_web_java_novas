<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}NoVas - Votre Parcours Etudiant{% endblock %}</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;500;600;700;800&family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Nunito', 'sans-serif'],
                        serif: ['Playfair Display', 'serif'],
                    },
                    colors: {
                        background: '#0f0e0c',
                        foreground: '#faf7f2',
                        primary: '#c75b39',
                        'primary-foreground': '#ffffff',
                        secondary: '#1a1917',
                        accent: '#7c9a6d',
                        muted: '#2a2825',
                        'muted-foreground': '#a8a29e',
                    }
                }
            }
        }
    </script>
    
    <style>
        .gradient-orb {
            background: radial-gradient(circle at 50% 50%, rgba(199, 91, 57, 0.15) 0%, rgba(124, 154, 109, 0.1) 40%, transparent 70%);
        }
        .glass-card {
            background: rgba(26, 25, 23, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
        .glow-primary {
            box-shadow: 0 0 60px rgba(199, 91, 57, 0.3);
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-background text-foreground font-sans antialiased overflow-x-hidden">
    {% block body %}{% endblock %}
    <script>
        lucide.createIcons();
        window.addEventListener('pageshow', function (event) {
            if (event.persisted) {
                window.location.reload();
            }
        });
    </script>
    {% block javascripts %}{% endblock %}
</body>
</html>
