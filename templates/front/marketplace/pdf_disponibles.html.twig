<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Produits disponibles - Marketplace</title>
    <style>
        @page {
            margin: 22px;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: Helvetica, Arial, sans-serif;
            background: #f6f4ef;
            color: #1f1c18;
        }

        .page {
            background: #f6f4ef;
        }

        .header {
            background: #1a1917;
            border: 1px solid #2f2b26;
            color: #fff;
            border-radius: 14px;
            padding: 16px 18px;
            margin-bottom: 14px;
        }

        .brand-row {
            width: 100%;
        }

        .brand {
            font-size: 22px;
            font-weight: 700;
            margin: 0;
            letter-spacing: 0.2px;
        }

        .chip {
            float: right;
            margin-top: -24px;
            padding: 4px 10px;
            border-radius: 999px;
            background: #c75b39;
            color: #fff;
            font-size: 10px;
            font-weight: 700;
        }

        .subtitle {
            font-size: 12px;
            color: #d0c6bb;
            margin: 6px 0 0 0;
        }

        .summary {
            margin-bottom: 14px;
        }

        .card {
            display: inline-block;
            width: 31.6%;
            margin-right: 1.2%;
            background: #fff;
            border: 1px solid #eadfd3;
            border-radius: 12px;
            padding: 10px 12px;
            box-sizing: border-box;
            vertical-align: top;
        }

        .card:last-child {
            margin-right: 0;
        }

        .card-label {
            font-size: 10px;
            color: #7d7267;
            margin: 0 0 5px 0;
            text-transform: uppercase;
            letter-spacing: 0.4px;
        }

        .card-value {
            font-size: 16px;
            font-weight: 700;
            color: #1f1c18;
            margin: 0;
        }

        .card-value.accent {
            color: #c75b39;
        }

        .table-wrap {
            background: #fff;
            border: 1px solid #eadfd3;
            border-radius: 14px;
            overflow: hidden;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead th {
            background: #1a1917;
            color: #faf7f2;
            text-align: left;
            font-size: 10px;
            padding: 11px 10px;
            border-bottom: 1px solid #2a2825;
            letter-spacing: 0.3px;
            text-transform: uppercase;
        }

        tbody td {
            font-size: 11px;
            padding: 11px 10px;
            border-bottom: 1px solid #f1e7db;
            vertical-align: top;
            line-height: 1.42;
        }

        tbody tr:nth-child(even) {
            background: #fcfaf7;
        }

        tbody tr:last-child td {
            border-bottom: none;
        }

        .status {
            display: inline-block;
            padding: 3px 8px;
            border-radius: 999px;
            font-size: 10px;
            font-weight: 700;
            background: #e8f5e9;
            color: #2e7d32;
        }

        .title-cell {
            font-weight: 700;
            color: #1f1c18;
            margin-bottom: 3px;
        }

        .category-cell {
            font-size: 10px;
            color: #6f665c;
        }

        .price {
            font-weight: 700;
            color: #c75b39;
            white-space: nowrap;
        }

        .description {
            color: #3b352e;
        }

        .empty {
            margin-top: 14px;
            background: #fff;
            border: 1px solid #e9e1d7;
            border-radius: 12px;
            padding: 18px;
            font-size: 12px;
            color: #6f665c;
        }

        .footer {
            margin-top: 10px;
            font-size: 10px;
            color: #8a7f72;
            text-align: right;
        }
    </style>
</head>
<body>
    <div class="page">
        <div class="header">
            <div class="brand-row">
                <h1 class="brand">Marketplace Report</h1>
                <span class="chip">Produits disponibles</span>
            </div>
            <p class="subtitle">Export catalogue produits - style professionnel</p>
        </div>

        {% set total_price = 0 %}
        {% for article in articles %}
            {% set total_price = total_price + article.prix %}
        {% endfor %}

        <div class="summary">
            <div class="card">
                <p class="card-label">Date export</p>
                <p class="card-value">{{ generated_at|date('d/m/Y H:i') }}</p>
            </div>
            <div class="card">
                <p class="card-label">Total produits</p>
                <p class="card-value">{{ articles|length }}</p>
            </div>
            <div class="card">
                <p class="card-label">Prix moyen</p>
                <p class="card-value accent">
                    {% if articles|length > 0 %}
                        {{ (total_price / articles|length)|number_format(2, '.', ' ') }} DT
                    {% else %}
                        0.00 DT
                    {% endif %}
                </p>
            </div>
        </div>

        {% if articles is empty %}
            <div class="empty">Aucun produit disponible pour le moment.</div>
        {% else %}
            <div class="table-wrap">
                <table>
                    <thead>
                        <tr>
                            <th style="width: 26%;">Produit</th>
                            <th style="width: 14%;">Categorie</th>
                            <th style="width: 12%;">Prix</th>
                            <th style="width: 36%;">Description</th>
                            <th style="width: 12%;">Statut</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for article in articles %}
                            <tr>
                                <td>
                                    <div class="title-cell">{{ article.titre }}</div>
                                    <div class="category-cell">Reference #{{ loop.index }}</div>
                                </td>
                                <td>{{ article.categorie }}</td>
                                <td class="price">{{ article.prix }} DT</td>
                                <td class="description">{{ article.description|length > 240 ? article.description|slice(0, 240) ~ '...' : article.description }}</td>
                                <td><span class="status">{{ article.statut|capitalize }}</span></td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}

        <div class="footer">
            NoVas Marketplace - Document genere automatiquement
        </div>
    </div>
</body>
</html>
