<!-- 2FA Management Section -->
<div class="bg-card border border-white/10 rounded-lg p-6 mb-6">
    <div class="flex items-start justify-between mb-4">
        <div>
            <h3 class="text-lg font-semibold mb-1">Authentification à Deux Facteurs</h3>
            <p class="text-sm text-muted-foreground">
                {% if app.user.isTwoFactorEnabled %}
                    Renforcez votre compte avec une protection supplémentaire
                {% else %}
                    Activez la 2FA pour sécuriser votre compte
                {% endif %}
            </p>
        </div>
        <div class="flex items-center gap-2">
            {% if app.user.isTwoFactorEnabled %}
                <div class="px-3 py-1 rounded-full bg-green-500/20 border border-green-500/30">
                    <span class="text-xs font-medium text-green-400">Activée</span>
                </div>
            {% else %}
                <div class="px-3 py-1 rounded-full bg-gray-500/20 border border-gray-500/30">
                    <span class="text-xs font-medium text-gray-400">Désactivée</span>
                </div>
            {% endif %}
        </div>
    </div>

    {% if app.user.isTwoFactorEnabled %}
        <!-- 2FA Enabled -->
        <div class="space-y-4">
            <div class="bg-green-500/10 border border-green-500/30 rounded-lg p-4">
                <div class="flex gap-3">
                    <i data-lucide="check-circle" class="w-5 h-5 text-green-400 flex-shrink-0 mt-0.5"></i>
                    <div>
                        <p class="text-sm text-green-100">
                            Votre compte est protégé par deux couches de sécurité. 
                            Vous devrez entrer un code d'authentification à chaque connexion.
                        </p>
                    </div>
                </div>
            </div>

            <div class="flex gap-3">
                <a href="{{ path('app_2fa_setup') }}" class="flex-1 px-4 py-2 bg-secondary border border-white/10 rounded-lg text-sm font-medium hover:bg-white/5 transition-colors flex items-center justify-center gap-2">
                    <i data-lucide="refresh-cw" class="w-4 h-4"></i>
                    Réinitialiser la clé
                </a>
                
                <form method="POST" action="{{ path('app_2fa_disable') }}" class="flex-1">
                    <button type="submit" class="w-full px-4 py-2 bg-rose-500/20 border border-rose-500/30 rounded-lg text-sm font-medium text-rose-400 hover:bg-rose-500/30 transition-colors" 
                        onclick="return confirm('Êtes-vous sûr? Votre compte sera moins sécurisé.')">
                        <i data-lucide="trash-2" class="w-4 h-4 inline mr-2"></i>
                        Désactiver 2FA
                    </button>
                </form>
            </div>
        </div>
    {% else %}
        <!-- 2FA Disabled -->
        <div class="space-y-4">
            <div class="bg-blue-500/10 border border-blue-500/30 rounded-lg p-4">
                <div class="flex gap-3">
                    <i data-lucide="info" class="w-5 h-5 text-blue-400 flex-shrink-0 mt-0.5"></i>
                    <div>
                        <p class="text-sm text-blue-100">
                            L'authentification à deux facteurs ajoute une couche de sécurité supplémentaire. 
                            En activant la 2FA, vous aurez besoin d'un code supplémentaire qui change chaque 30 secondes 
                            pour vous connecter à votre compte.
                        </p>
                    </div>
                </div>
            </div>

            <a href="{{ path('app_2fa_setup') }}" class="w-full px-4 py-2 bg-primary text-white rounded-lg font-medium hover:bg-primary/90 transition-colors flex items-center justify-center gap-2">
                <i data-lucide="shield-check" class="w-4 h-4"></i>
                Activer la 2FA maintenant
            </a>
        </div>
    {% endif %}
</div>
