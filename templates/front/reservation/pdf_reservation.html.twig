<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Réservation #{{ reservation.id }}</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 12px; padding: 24px; color: #333; }
        h1 { font-size: 20px; margin-bottom: 20px; color: #c75b39; border-bottom: 2px solid #c75b39; padding-bottom: 8px; }
        .block { margin-bottom: 16px; }
        .label { font-weight: bold; color: #555; }
        .badge { display: inline-block; padding: 4px 10px; border-radius: 6px; font-size: 11px; font-weight: bold; }
        .badge-soft { background: #e0f2fe; color: #0369a1; }
        .badge-hard { background: #ede9fe; color: #5b21b6; }
        .badge-attente { background: #fef3c7; color: #b45309; }
        .badge-confirme { background: #d1fae5; color: #047857; }
        .footer { margin-top: 32px; font-size: 10px; color: #888; }
    </style>
</head>
<body>
    <h1>Confirmation de réservation – NoVas</h1>

    <div class="block">
        <span class="label">N° réservation :</span> {{ reservation.id }}
    </div>

    <div class="block">
        <span class="label">Atelier :</span> {{ reservation.atelier ? reservation.atelier.titreAtelier : '—' }}
    </div>

    <div class="block">
        <span class="label">Type :</span> {{ reservation.atelier ? reservation.atelier.typeAtelier|capitalize : '—' }}
    </div>

    <div class="block">
        <span class="label">Contexte :</span>
        {% if reservation.atelier %}
            <span class="badge {{ reservation.atelier.contexteAtelier == 0 ? 'badge-soft' : 'badge-hard' }}">
                {{ reservation.atelier.contexteAtelier == 0 ? 'Soft Skill' : 'Hard Skill' }}
            </span>
        {% else %}
            —
        {% endif %}
    </div>

    <div class="block">
        <span class="label">Date de l'atelier :</span>
        {{ reservation.atelier and reservation.atelier.dateAtelier ? reservation.atelier.dateAtelier|date('d/m/Y') : '—' }}
    </div>

    <div class="block">
        <span class="label">Participant :</span> {{ reservation.nomUser }}
    </div>

    <div class="block">
        <span class="label">Email :</span> {{ reservation.emailUser }}
    </div>

    <div class="block">
        <span class="label">Statut :</span>
        <span class="badge {{ reservation.statutReservation == 1 ? 'badge-confirme' : 'badge-attente' }}">
            {{ reservation.statutReservation == 1 ? 'Confirmée' : 'En attente' }}
        </span>
    </div>

    {% if reservation.commentaireReservation %}
    <div class="block">
        <span class="label">Commentaire :</span> {{ reservation.commentaireReservation }}
    </div>
    {% endif %}

    <div class="footer">
        Document généré le {{ "now"|date("d/m/Y H:i") }} – NoVas
    </div>
</body>
</html>
