### MailerSend (local override)
# Keep real secrets out of .env (versioned).
# Put the real key in .env.local on each machine.
MAILER_DSN=mailersend+api://CHANGE_ME@default
MAILER_FROM="NOVAS <no-reply@test-dnvo4d9kmk3g5r86.mlsender.net>"
