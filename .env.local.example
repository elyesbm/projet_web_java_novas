### Brevo (local override)
# Keep real secrets out of .env (versioned).
# Put the real key in .env.local on each machine.
MAILER_DSN=brevo+api://CHANGE_ME@default
MAILER_FROM="NOVAS <no-reply@example.com>"

### AI (Gemini or Hugging Face)
# Choose provider: gemini or huggingface
AI_PROVIDER=huggingface
# Put your real API key here (Google AI Studio or Hugging Face)
AI_API_KEY=CHANGE_ME
# Optional models
AI_HF_MODEL=katanemo/Arch-Router-1.5B
AI_GEMINI_MODEL=gemini-1.5-flash-latest

### External translation (Ateliers)
# Default: MyMemory (gratuit, sans clé) — possible de passer à LibreTranslate.
TRANSLATE_PROVIDER=mymemory
# Optionnel (MyMemory): mets ton email pour de meilleurs quotas.
TRANSLATE_API_EMAIL=
# Option LibreTranslate (si TRANSLATE_PROVIDER=libretranslate)
TRANSLATE_LIBRE_BASE_URL=
TRANSLATE_LIBRE_API_KEY=

### Text-to-Speech (Ateliers)
# Provider: voicerss (VoiceRSS)
TTS_PROVIDER=voicerss
# Clé VoiceRSS (gratuite) : https://www.voicerss.org/
VOICERSS_API_KEY=

# Si tu as une erreur SSL "unable to get local issuer certificate" (souvent sur Windows),
# tu as 2 choix :
# 1) (Recommandé) fournir un fichier CA :
#    VOICERSS_CAFILE=C:\\chemin\\vers\\cacert.pem
# 2) (Dépannage) désactiver la vérification SSL (à éviter en prod) :
#    VOICERSS_VERIFY_SSL=0

# Option (si tu veux quand même Google Cloud TTS):
# GOOGLE_TTS_API_KEY=
